package com.gmproxy.Util;

/**
 * Hey, even if it is just one constant, might as well add it to its class, right?
 * Probably I should add all intent.putExtra tags here, but we'll wait for 0.2 for that, we still have to deliver this on time.
 */
public class Constants {
    public static String DBNAME = "pastilarma";
    public static final int DESIGN_WIDTH = 1080 ;
    public static final int DESIGN_HEIGHT = 2400 ;
}
